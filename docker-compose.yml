version: "2"
services:
  goof:
    build: .
    container_name: goof
    environment:
      - DOCKER=1
      # Match the goof-mysql credentials defined below so the app can connect.
      - MYSQL_HOST=goof-mysql
      - MYSQL_USER=root
      - MYSQL_PASSWORD=${MYSQL_PASSWORD:-root}
      - MYSQL_DATABASE=acme
    ports:
      - "3001:3001"
      - "9229:9229"
    links:
      - goof-mongo
      - good-mysql
    depends_on:
      - goof-mongo
      - good-mysql
  goof-mongo:
    container_name: goof-mongo
    image: mongo:3
    ports:
      - "27017:27017"
  good-mysql:
    container_name: goof-mysql
    image: mysql:5
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_PASSWORD:-root}
      MYSQL_DATABASE: acme
    ports:
      - "3306:3306"
